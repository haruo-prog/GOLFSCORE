name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android API 36 platform
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build debug APK
        id: build_apk
        run: |
          set +e
          gradle assembleDebug --stacktrace 2>&1 | tee build-output.log
          BUILD_EXIT=${PIPESTATUS[0]}
          echo "exit_code=${BUILD_EXIT}" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: NK-Score-Manager-build-log
          path: build-output.log

      - name: Stop when build failed
        if: steps.build_apk.outputs.exit_code != '0'
        run: exit ${{ steps.build_apk.outputs.exit_code }}

      - name: Prepare release APK
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/app-debug.apk release/NK-Score-Manager-v1.3.0-debug.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: NK-Score-Manager-debug-apk
          path: release/NK-Score-Manager-v1.3.0-debug.apk

      - name: Commit APK to release folder
        if: github.event_name != 'pull_request'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add release/NK-Score-Manager-v1.3.0-debug.apk
          git diff --cached --quiet && echo "No APK changes to commit" || git commit -m "Add latest debug APK [skip ci]"
          git push
